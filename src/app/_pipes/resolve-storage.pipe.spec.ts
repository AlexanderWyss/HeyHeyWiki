import { ResolveStoragePipe } from './resolve-storage.pipe';

describe('ResolveStoragePipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveStoragePipe();
    expect(pipe).toBeTruthy();
  });
});
